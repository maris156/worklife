"use client";

import Image from "next/image";
import { useMemo, useState } from "react";
import { ArrowRight, CalendarDays, Check, ChevronDown, Clock3, Compass, Copy, DollarSign, Globe2, Heart, MapPin, Menu, MoreHorizontal, Navigation, Send, Share2, Sparkles, WandSparkles, X, Zap } from "lucide-react";
import { generateTrip, modifyItinerary, optimizeBudget, replaceActivity } from "@/lib/ai-service";
import { kyotoTrip, type Activity, type Trip } from "@/lib/travel-data";

type Stage = "landing" | "create" | "dashboard";
const interests = ["Culture", "Food & drink", "Nature", "Design", "Wellness"];
const styles = ["Balanced", "Slow & easy", "Curious & active", "Food first"];

export default function Home() {
  const [stage, setStage] = useState<Stage>("landing");
  const [step, setStep] = useState(1);
  const [trip, setTrip] = useState<Trip>(kyotoTrip);
  const [destination, setDestination] = useState("Kyoto");
  const [travelers, setTravelers] = useState(2);
  const [budget, setBudget] = useState(4200);
  const [selectedInterests, setSelectedInterests] = useState(["Culture", "Food & drink", "Nature"]);
  const [style, setStyle] = useState("Balanced");
  const [loading, setLoading] = useState(false);
  const [notice, setNotice] = useState("");
  const [command, setCommand] = useState("");
  const [assistantOpen, setAssistantOpen] = useState(false);

  const totalSpend = useMemo(() => trip.days.flatMap((day) => day.activities).reduce((sum, activity) => sum + activity.cost, 0), [trip]);
  const budgetPercent = Math.min(100, Math.round((totalSpend / trip.budget) * 100));

  async function createTrip() {
    setLoading(true);
    await new Promise((resolve) => setTimeout(resolve, 900));
    const generated = await generateTrip({ destination, dates: "Oct 14 – 20, 2025", travelers, budget, currency: "USD", interests: selectedInterests, style, constraints: "No early mornings" });
    setTrip(generated); setStage("dashboard"); setLoading(false); setNotice("Your itinerary is ready");
  }
  async function runCommand(instruction = command) {
    if (!instruction.trim()) return;
    setLoading(true); setNotice("");
    await new Promise((resolve) => setTimeout(resolve, 650));
    setTrip(await modifyItinerary(trip, instruction)); setCommand(""); setLoading(false); setNotice("Trip updated — preferences preserved");
  }
  async function optimize() { setLoading(true); await new Promise((resolve) => setTimeout(resolve, 650)); setTrip(await optimizeBudget(trip)); setLoading(false); setNotice("Trip optimized — 18% saved"); }
  async function replace(id: string) { setLoading(true); await new Promise((resolve) => setTimeout(resolve, 500)); setTrip(await replaceActivity(trip, id)); setLoading(false); setNotice("Found a better local match"); }
  function toggleInterest(interest: string) { setSelectedInterests((current) => current.includes(interest) ? current.filter((item) => item !== interest) : [...current, interest]); }
  function notify(message: string) { setNotice(message); setTimeout(() => setNotice(""), 2600); }

  return <main className="page-shell">
    {stage === "landing" && <Landing onStart={() => { setStage("create"); setStep(1); }} onExample={() => setStage("dashboard")} />}
    {stage === "create" && <CreateFlow step={step} setStep={setStep} destination={destination} setDestination={setDestination} travelers={travelers} setTravelers={setTravelers} budget={budget} setBudget={setBudget} selectedInterests={selectedInterests} toggleInterest={toggleInterest} style={style} setStyle={setStyle} loading={loading} onGenerate={createTrip} onBack={() => setStage("landing")} />}
    {stage === "dashboard" && <Dashboard trip={trip} totalSpend={totalSpend} budgetPercent={budgetPercent} loading={loading} notice={notice} command={command} setCommand={setCommand} assistantOpen={assistantOpen} setAssistantOpen={setAssistantOpen} onCommand={runCommand} onOptimize={optimize} onReplace={replace} onNotify={notify} onNew={() => { setStage("create"); setStep(1); }} />}
  </main>;
}

function Topbar({ onStart, compact = false }: { onStart?: () => void; compact?: boolean }) {
  return <header className={`mx-auto flex w-full max-w-[1240px] items-center justify-between px-5 py-5 lg:px-8 ${compact ? "border-b border-[var(--line)]" : ""}`}><button className="flex items-center gap-2 text-left focus-ring" onClick={onStart}><span className="grid h-8 w-8 place-items-center rounded-xl bg-[var(--moss)] text-white"><Compass size={17} /></span><span className="text-[17px] font-semibold tracking-[-.03em]">wanderly<span className="text-[var(--saffron)]">.</span></span></button><nav className="hidden items-center gap-7 text-sm text-[var(--muted)] md:flex"><a href="#how">How it works</a><a href="#inspiration">Inspiration</a><a href="#saved">Saved trips</a></nav><button onClick={onStart} className="focus-ring hidden items-center gap-2 rounded-full border border-[var(--line)] bg-white px-4 py-2 text-sm font-medium text-[var(--moss-dark)] shadow-sm transition hover:border-[var(--moss)] sm:flex">My trips <ChevronDown size={14} /></button><button className="focus-ring rounded-lg p-2 text-[var(--moss-dark)] md:hidden" aria-label="Open menu"><Menu size={21} /></button></header>;
}

function Landing({ onStart, onExample }: { onStart: () => void; onExample: () => void }) {
  return <div className="soft-grid min-h-screen"><Topbar onStart={onStart} /><section className="mx-auto grid max-w-[1240px] items-center gap-12 px-5 pb-20 pt-12 lg:grid-cols-[1.03fr_.97fr] lg:px-8 lg:pb-28 lg:pt-20"><div><div className="mb-7 flex items-center gap-2 text-sm font-medium text-[var(--moss)]"><span className="h-2 w-2 rounded-full bg-[var(--saffron)]" /> The thoughtful way to travel</div><h1 className="serif max-w-2xl text-[clamp(3.4rem,7vw,6.9rem)] leading-[.92] tracking-[-.07em] text-[var(--moss-dark)]">Go somewhere<br /><em className="text-[var(--saffron)]">wonderful.</em></h1><p className="mt-8 max-w-lg text-lg leading-8 text-[var(--muted)]">Your own personal travel designer. Build a trip that feels like you — not a checklist.</p><div className="mt-9 flex flex-wrap items-center gap-3"><button onClick={onStart} className="focus-ring flex items-center gap-3 rounded-full bg-[var(--moss)] px-6 py-3.5 font-medium text-white shadow-lg shadow-[#3e594b]/20 transition hover:-translate-y-0.5 hover:bg-[var(--moss-dark)]">Plan my trip <ArrowRight size={17} /></button><button onClick={onExample} className="focus-ring rounded-full px-5 py-3.5 text-sm font-medium text-[var(--moss)] transition hover:bg-white">See an example trip</button></div><div className="mt-12 flex items-center gap-3 text-sm text-[var(--muted)]"><div className="flex -space-x-2"><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[var(--paper)] bg-[#e5b49a] text-xs">M</span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[var(--paper)] bg-[#9db8a4] text-xs">J</span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[var(--paper)] bg-[#d8c38b] text-xs">S</span></div><span>Join 12,000+ curious travelers</span><span className="text-[var(--saffron)]">★★★★★</span></div></div><div className="relative mx-auto w-full max-w-[520px]"><div className="absolute -left-5 top-10 z-10 hidden rounded-2xl border border-[var(--line)] bg-white p-4 shadow-xl sm:block"><div className="mb-2 flex items-center gap-2 text-xs font-medium text-[var(--muted)]"><Sparkles size={14} className="text-[var(--saffron)]" /> YOUR TRIP, YOUR WAY</div><p className="serif text-lg text-[var(--moss-dark)]">Slow mornings in<br />Kyoto</p></div><div className="overflow-hidden rounded-[32px] border-8 border-white shadow-2xl shadow-[#3e594b]/15"><Image src="https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?auto=format&fit=crop&w=1200&q=85" alt="Traditional Kyoto street in autumn" width={1200} height={1600} priority className="h-[520px] w-full object-cover lg:h-[620px]" /></div><div className="absolute -bottom-5 right-4 flex items-center gap-3 rounded-2xl border border-[var(--line)] bg-white px-4 py-3 shadow-xl"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[var(--sage)] text-[var(--moss)]"><Heart size={17} fill="currentColor" /></div><div><p className="text-xs text-[var(--muted)]">Made for you</p><p className="text-sm font-semibold text-[var(--moss-dark)]">7 days · 2 travelers</p></div></div></div></section><section id="how" className="mx-auto max-w-[1240px] border-t border-[var(--line)] px-5 py-16 lg:px-8"><div className="grid gap-8 md:grid-cols-3"><Feature num="01" icon={<Sparkles size={18} />} title="Tell us your rhythm" text="Your pace, your passions, your non-negotiables. We listen." /><Feature num="02" icon={<WandSparkles size={18} />} title="Get a trip that fits" text="A thoughtful day-by-day plan, made around the way you actually travel." /><Feature num="03" icon={<Zap size={18} />} title="Make it yours" text="Change anything in a sentence. Your AI travel partner keeps up." /></div></section></div>;
}
function Feature({ num, icon, title, text }: { num: string; icon: React.ReactNode; title: string; text: string }) { return <div className="border-l border-[var(--line)] pl-5"><div className="mb-7 flex items-center justify-between text-xs text-[var(--muted)]"><span>{num}</span><span className="text-[var(--saffron)]">{icon}</span></div><h2 className="serif text-2xl text-[var(--moss-dark)]">{title}</h2><p className="mt-3 max-w-xs text-sm leading-6 text-[var(--muted)]">{text}</p></div>; }

function CreateFlow({ step, setStep, destination, setDestination, travelers, setTravelers, budget, setBudget, selectedInterests, toggleInterest, style, setStyle, loading, onGenerate, onBack }: { step: number; setStep: (step: number) => void; destination: string; setDestination: (value: string) => void; travelers: number; setTravelers: (value: number) => void; budget: number; setBudget: (value: number) => void; selectedInterests: string[]; toggleInterest: (value: string) => void; style: string; setStyle: (value: string) => void; loading: boolean; onGenerate: () => void; onBack: () => void }) {
  return <div className="min-h-screen"><Topbar onStart={onBack} compact /><div className="mx-auto max-w-3xl px-5 pb-16 pt-10 lg:pt-16"><div className="mb-12 flex items-center justify-between"><div><p className="text-sm font-medium text-[var(--saffron)]">LET&apos;S MAKE A PLAN</p><h1 className="serif mt-2 text-4xl tracking-[-.04em] text-[var(--moss-dark)]">A little about your trip</h1></div><span className="text-sm text-[var(--muted)]">Step {step} of 3</span></div><div className="mb-12 h-1 overflow-hidden rounded-full bg-[#e5eae3]"><div className="h-full rounded-full bg-[var(--saffron)] transition-all duration-500" style={{ width: `${step / 3 * 100}%` }} /></div>{step === 1 && <div className="space-y-8"><div><label className="mb-2 block text-sm font-medium">Where would you like to go?</label><div className="relative"><MapPin className="absolute left-4 top-4 text-[var(--saffron)]" size={20} /><input value={destination} onChange={(event) => setDestination(event.target.value)} className="focus-ring w-full rounded-2xl border border-[var(--line)] bg-white py-4 pl-12 pr-4 text-lg outline-none transition focus:border-[var(--moss)]" placeholder="A destination or ‘surprise me’" /></div><p className="mt-2 text-sm text-[var(--muted)]">Try a city, a country, or tell us what you&apos;re dreaming of.</p></div><div><label className="mb-3 block text-sm font-medium">When are you going?</label><div className="grid gap-3 sm:grid-cols-2"><div className="flex items-center gap-3 rounded-2xl border border-[var(--line)] bg-white px-4 py-4"><CalendarDays size={18} className="text-[var(--saffron)]" /><span className="text-sm">Oct 14, 2025</span></div><div className="flex items-center gap-3 rounded-2xl border border-[var(--line)] bg-white px-4 py-4"><Clock3 size={18} className="text-[var(--saffron)]" /><span className="text-sm">7 days</span></div></div></div><div><label className="mb-3 block text-sm font-medium">Who&apos;s coming?</label><div className="flex w-fit items-center gap-4 rounded-2xl border border-[var(--line)] bg-white p-2"><button className="focus-ring grid h-9 w-9 place-items-center rounded-xl border border-[var(--line)] text-lg" onClick={() => setTravelers(Math.max(1, travelers - 1))}>−</button><span className="min-w-28 text-center text-sm">{travelers} travelers</span><button className="focus-ring grid h-9 w-9 place-items-center rounded-xl bg-[var(--moss)] text-lg text-white" onClick={() => setTravelers(travelers + 1)}>+</button></div></div></div>}{step === 2 && <div className="space-y-10"><div><label className="mb-3 block text-sm font-medium">What are you into?</label><div className="flex flex-wrap gap-2">{interests.map((interest) => <button key={interest} onClick={() => toggleInterest(interest)} className={`pill focus-ring ${selectedInterests.includes(interest) ? "active" : ""}`}>{selectedInterests.includes(interest) && <Check size={14} className="mr-1 inline" />}{interest}</button>)}</div></div><div><label className="mb-3 block text-sm font-medium">Your travel style</label><div className="grid gap-3 sm:grid-cols-2">{styles.map((item) => <button key={item} onClick={() => setStyle(item)} className={`focus-ring rounded-2xl border p-4 text-left transition ${style === item ? "border-[var(--moss)] bg-[var(--sage)]" : "border-[var(--line)] bg-white hover:border-[var(--moss)]"}`}><span className="font-medium text-[var(--moss-dark)]">{item}</span><span className="mt-1 block text-sm text-[var(--muted)]">{item === "Balanced" ? "A little of everything, at a human pace" : "We&apos;ll shape the days around this feeling"}</span></button>)}</div></div><div><label className="mb-3 block text-sm font-medium">Anything we should know?</label><textarea className="focus-ring min-h-28 w-full resize-none rounded-2xl border border-[var(--line)] bg-white p-4 outline-none" placeholder="Dietary needs, mobility notes, must-sees…" /></div></div>}{step === 3 && <div className="space-y-8"><div><label className="mb-2 block text-sm font-medium">What&apos;s your trip budget?</label><div className="relative"><DollarSign className="absolute left-4 top-4 text-[var(--saffron)]" size={19} /><input type="number" value={budget} onChange={(event) => setBudget(Number(event.target.value))} className="focus-ring w-full rounded-2xl border border-[var(--line)] bg-white py-4 pl-11 pr-4 text-lg outline-none focus:border-[var(--moss)]" /></div><p className="mt-2 text-sm text-[var(--muted)]">Total for {travelers} travelers, excluding flights.</p></div><div className="card bg-[#edf4ec] p-6"><div className="flex items-start gap-4"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-white text-[var(--saffron)]"><Sparkles size={19} /></div><div><p className="font-medium text-[var(--moss-dark)]">We&apos;ll make every yen count</p><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Your plan will balance memorable experiences with smart, realistic spending — and show you exactly where it goes.</p></div></div></div></div>}<div className="mt-14 flex items-center justify-between border-t border-[var(--line)] pt-6"><button onClick={() => step === 1 ? onBack() : setStep(step - 1)} className="focus-ring text-sm font-medium text-[var(--muted)]">← Back</button>{step < 3 ? <button onClick={() => setStep(step + 1)} className="focus-ring flex items-center gap-2 rounded-full bg-[var(--moss)] px-6 py-3 font-medium text-white">Continue <ArrowRight size={16} /></button> : <button onClick={onGenerate} disabled={loading} className="focus-ring flex items-center gap-2 rounded-full bg-[var(--moss)] px-6 py-3 font-medium text-white disabled:opacity-60">{loading ? "Designing your trip…" : "Create my trip"} {!loading && <WandSparkles size={16} />}</button>}</div></div></div>;
}

function Dashboard({ trip, totalSpend, budgetPercent, loading, notice, command, setCommand, assistantOpen, setAssistantOpen, onCommand, onOptimize, onReplace, onNotify, onNew }: { trip: Trip; totalSpend: number; budgetPercent: number; loading: boolean; notice: string; command: string; setCommand: (value: string) => void; assistantOpen: boolean; setAssistantOpen: (value: boolean) => void; onCommand: (instruction?: string) => void; onOptimize: () => void; onReplace: (id: string) => void; onNotify: (message: string) => void; onNew: () => void }) {
  return <div className="min-h-screen"><Topbar onStart={onNew} compact /><div className="mx-auto max-w-[1240px] px-5 pb-20 pt-7 lg:px-8"><div className="mb-8 flex flex-wrap items-end justify-between gap-5"><div><div className="mb-3 flex items-center gap-2 text-sm text-[var(--muted)]"><button onClick={onNew} className="hover:text-[var(--moss)]">My trips</button><span>/</span><span>{trip.destination}</span></div><h1 className="serif text-5xl tracking-[-.05em] text-[var(--moss-dark)]">{trip.destination}<span className="text-[var(--saffron)]">.</span></h1><div className="mt-3 flex flex-wrap gap-4 text-sm text-[var(--muted)]"><span className="flex items-center gap-1.5"><CalendarDays size={15} /> {trip.dates}</span><span className="flex items-center gap-1.5"><Globe2 size={15} /> {trip.travelers} travelers</span><span className="flex items-center gap-1.5"><Navigation size={15} /> {trip.pace} pace</span></div></div><div className="flex gap-2"><button className="focus-ring rounded-full border border-[var(--line)] bg-white p-3 text-[var(--moss)]" onClick={() => onNotify("Trip link copied to clipboard")} aria-label="Share trip"><Share2 size={17} /></button><button className="focus-ring flex items-center gap-2 rounded-full bg-[var(--moss)] px-4 py-2.5 text-sm font-medium text-white" onClick={() => setAssistantOpen(!assistantOpen)}><Sparkles size={16} /> Ask Wanderly</button></div></div><div className="grid gap-5 lg:grid-cols-[1fr_340px]"><div><div className="mb-5 flex items-center justify-between"><h2 className="text-sm font-semibold uppercase tracking-[.12em] text-[var(--muted)]">Your itinerary</h2><button onClick={() => onNotify("Trip saved") } className="flex items-center gap-1.5 text-sm font-medium text-[var(--moss)]"><Check size={15} /> Saved</button></div>{trip.days.map((day) => <Day key={day.id} day={day} onReplace={onReplace} />)}</div><aside className="space-y-5"><div className="card overflow-hidden"><div className="h-36 bg-cover bg-center" style={{ backgroundImage: `url(${trip.days[0].activities[0].image})` }} /><div className="p-5"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[var(--muted)]">Trip overview</p><MoreHorizontal size={17} className="text-[var(--muted)]" /></div><div className="mt-5 grid grid-cols-2 gap-y-5"><Stat label="Days" value="7" /><Stat label="Travelers" value={String(trip.travelers)} /><Stat label="Est. spend" value={`$${totalSpend.toLocaleString()}`} /><Stat label="Per person" value={`$${Math.round(totalSpend / trip.travelers).toLocaleString()}`} /></div></div></div><div className="card p-5"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[var(--muted)]">Budget</p><button onClick={onOptimize} className="flex items-center gap-1 text-xs font-semibold text-[var(--saffron)]"><Zap size={13} /> Optimize</button></div><div className="mt-5 flex items-end justify-between"><p className="text-3xl font-semibold tracking-[-.05em] text-[var(--moss-dark)]">${totalSpend.toLocaleString()}</p><p className="pb-1 text-sm text-[var(--muted)]">of ${trip.budget.toLocaleString()}</p></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-[#edf0ea]"><div className="h-full rounded-full bg-[var(--saffron)] transition-all duration-500" style={{ width: `${budgetPercent}%` }} /></div><div className="mt-4 flex justify-between text-xs text-[var(--muted)]"><span>On track</span><span>{100 - budgetPercent}% remaining</span></div><div className="mt-6 space-y-3 border-t border-[var(--line)] pt-4"><BudgetRow label="Stay" value="$1,540" width="58%" color="var(--moss)" /><BudgetRow label="Food & drink" value="$620" width="24%" color="var(--saffron)" /><BudgetRow label="Experiences" value="$380" width="14%" color="var(--plum)" /></div></div><div className="card bg-[var(--moss-dark)] p-5 text-white"><div className="flex items-center gap-2 text-sm font-medium"><Sparkles size={16} className="text-[#e9bb76]" /> Trip assistant</div><p className="serif mt-5 text-2xl leading-tight">What would make this trip feel more like you?</p><button onClick={() => setAssistantOpen(true)} className="mt-5 flex items-center gap-2 text-sm font-medium text-[#e9bb76]">Start a conversation <ArrowRight size={15} /></button></div></aside></div></div>{assistantOpen && <Assistant command={command} setCommand={setCommand} loading={loading} onClose={() => setAssistantOpen(false)} onCommand={onCommand} />}{notice && <div className="fixed bottom-5 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-full bg-[var(--moss-dark)] px-5 py-3 text-sm text-white shadow-2xl"><Check size={16} className="text-[#e9bb76]" /> {notice}</div>}</div>;
}
function Stat({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-[var(--muted)]">{label}</p><p className="mt-1 text-lg font-semibold text-[var(--moss-dark)]">{value}</p></div>; }
function BudgetRow({ label, value, width, color }: { label: string; value: string; width: string; color: string }) { return <div><div className="mb-1.5 flex justify-between text-xs"><span className="text-[var(--muted)]">{label}</span><span className="font-medium text-[var(--moss-dark)]">{value}</span></div><div className="h-1.5 rounded-full bg-[#edf0ea]"><div className="h-full rounded-full" style={{ width, background: color }} /></div></div>; }
function Day({ day, onReplace }: { day: Trip["days"][number]; onReplace: (id: string) => void }) { return <section className="mb-9"><div className="mb-4 flex items-baseline gap-3"><h3 className="serif text-2xl text-[var(--moss-dark)]">{day.label}</h3><span className="text-sm text-[var(--muted)]">{day.date}</span></div><div className="relative ml-3 border-l border-dashed border-[#cbd8cc] pl-7">{day.activities.map((activity) => <ActivityCard activity={activity} key={activity.id} onReplace={onReplace} />)}</div></section>; }
function ActivityCard({ activity, onReplace }: { activity: Activity; onReplace: (id: string) => void }) { return <article className="activity-card group relative mb-4 grid overflow-hidden rounded-2xl border border-[var(--line)] bg-white transition hover:border-[#b6c9b9] hover:shadow-[var(--shadow)] sm:grid-cols-[148px_1fr]"><span className="absolute -left-[34px] top-7 h-3 w-3 rounded-full border-[3px] border-[var(--paper)] bg-[var(--saffron)]" /><div className="h-40 overflow-hidden sm:h-full"><Image src={activity.image} alt="" width={900} height={600} className="activity-image h-full w-full object-cover" /></div><div className="p-4 sm:p-5"><div className="mb-2 flex flex-wrap items-center gap-2 text-xs"><span className="font-semibold text-[var(--saffron)]">{activity.time}</span><span className="text-[var(--muted)]">· {activity.duration}</span><span className="rounded-full bg-[#f0f3ee] px-2 py-1 text-[var(--muted)]">{activity.category}</span></div><h4 className="text-lg font-semibold tracking-[-.02em] text-[var(--moss-dark)]">{activity.title}</h4><p className="mt-1.5 max-w-xl text-sm leading-6 text-[var(--muted)]">{activity.description}</p><div className="mt-4 flex flex-wrap items-center justify-between gap-3 text-xs text-[var(--muted)]"><span className="flex items-center gap-1"><MapPin size={13} /> {activity.location}</span><span className="font-medium text-[var(--moss)]">{activity.cost === 0 ? "Free" : `~$${activity.cost} / person`}</span></div><div className="mt-3 flex gap-3 border-t border-[var(--line)] pt-3 opacity-100 sm:opacity-0 sm:transition group-hover:opacity-100"><button onClick={() => onReplace(activity.id)} className="flex items-center gap-1 text-xs font-medium text-[var(--moss)]"><WandSparkles size={13} /> Replace with AI</button><button className="flex items-center gap-1 text-xs font-medium text-[var(--muted)]"><Copy size={13} /> Duplicate</button></div></div></article>; }
function Assistant({ command, setCommand, loading, onClose, onCommand }: { command: string; setCommand: (value: string) => void; loading: boolean; onClose: () => void; onCommand: (instruction?: string) => void }) { const suggestions = ["Make this cheaper", "Add more local experiences", "Make the trip more relaxed"]; return <div className="fixed inset-0 z-40 bg-[#203429]/20 backdrop-blur-[2px]"><div className="absolute bottom-0 right-0 h-[min(680px,calc(100vh-24px))] w-full max-w-[440px] rounded-t-[28px] bg-[#f8faf6] p-5 shadow-2xl sm:bottom-5 sm:right-5 sm:rounded-[28px]"><div className="flex items-center justify-between border-b border-[var(--line)] pb-4"><div className="flex items-center gap-2"><div className="grid h-8 w-8 place-items-center rounded-xl bg-[var(--moss)] text-white"><Sparkles size={15} /></div><div><p className="font-semibold text-[var(--moss-dark)]">Wanderly assistant</p><p className="text-xs text-[var(--muted)]">Knows your whole trip</p></div></div><button className="focus-ring rounded-lg p-2 text-[var(--muted)]" onClick={onClose} aria-label="Close assistant"><X size={18} /></button></div><div className="flex h-[calc(100%-145px)] flex-col justify-end gap-4 overflow-y-auto py-5"><div className="max-w-[82%] rounded-2xl rounded-bl-sm bg-white p-4 text-sm leading-6 text-[var(--moss-dark)] shadow-sm">I&apos;m here to help shape this trip. Ask me to change the pace, swap an activity, or rebalance the budget.</div><div className="ml-auto max-w-[82%] rounded-2xl rounded-br-sm bg-[var(--moss)] p-4 text-sm leading-6 text-white">Try one of these to get started:</div><div className="flex flex-wrap justify-end gap-2">{suggestions.map((suggestion) => <button key={suggestion} onClick={() => onCommand(suggestion)} className="pill text-xs">{suggestion}</button>)}</div></div><form className="flex gap-2 border-t border-[var(--line)] pt-4" onSubmit={(event) => { event.preventDefault(); onCommand(); }}><input value={command} onChange={(event) => setCommand(event.target.value)} className="focus-ring min-w-0 flex-1 rounded-xl border border-[var(--line)] bg-white px-4 py-3 text-sm outline-none" placeholder="Tell me what to change…" /><button disabled={loading} className="focus-ring grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[var(--saffron)] text-white disabled:opacity-60" aria-label="Send request"><Send size={17} /></button></form></div></div>; }
